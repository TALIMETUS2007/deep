{
  "name": "deep-backend",
  "version": "1.0.0",
  "description": "Express backend for Deep — serves the site and proxies Deep AI chat to Claude",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
